# Which bridge to route through: "openai", "claude", or "deepseek". Defaults to "openai".
PROVIDER=claude

# Where whatsapp-mcp-server is running (see ../../mcp-servers/whatsapp-mcp-server)
WHATSAPP_MCP_URL=http://localhost:4100

# Port for `npm run serve` / `npm run dev:serve` (the AG-UI HTTP server + chat UI).
# Not used by `npm run dev` (the WhatsApp self-message listener).
PORT=3100

# Needed when PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1

# Needed when PROVIDER=claude
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5

# Needed when PROVIDER=deepseek
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-chat
